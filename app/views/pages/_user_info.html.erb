<div class="user_info">
  <a href="<%= user_path(current_user) %>">
    <%= gravatar_for current_user, :user => 30 %>
    <span class="user_name">
      <%= h current_user.name %>
    </span>
    <span class="microposts">
      <%= pluralize(current_user.microposts.count, "post") %>
    </span>
  </a>
</div>
